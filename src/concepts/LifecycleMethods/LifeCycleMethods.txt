we can classify methods in 4 cases

Mounting-When an instance of a component is being created and inserted into the DOM
Updating-When a component is being rerendered as a result of changes to either its props or state
Unmounting-When a component is removed from DOM
Error Handling-When there is a Error during rendering, in a lifecycle method

ComponentDidMount()-Invoked immediately after a component has been rendered. called only once